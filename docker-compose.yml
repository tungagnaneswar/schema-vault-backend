services:

  # Spring Boot backend
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: schema-vault-backend
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
